import React, {Component, PropTypes} from 'react';

export default class BaseSelector extends Component {
    render(){
        return (
            <div>
                BaseSelector
            </div>
        );
    }
}
