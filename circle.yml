machine:
  node:
    version: 6.9.1

test:
  override:
    - "true"

deployment:
  master:
    branch: ts-master
    commands:
      - npm run publish-storybook

general:
  branches:
    only:
      - ts-master
    ignore:
      - gh-pages
